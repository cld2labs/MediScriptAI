# Root context is not used by service Dockerfiles (each service has its own context).
# Kept minimal if someone runs `docker build` from the repo root by mistake.
.git
**/node_modules
**/.next
**/__pycache__
**/.venv
**/.env
**/.env.*
